---
title: '基于OpenMC官方镜像的Claude code安装与配置'
date: 2026-03-12
permalink: /posts/2026/12/CC-installation-and-configuration-based-on-OpenMC-official-image/
tags:
  - Claude code
  - 环境搭建
  - AI使用
categories:
  - 环境配置
---
# 基于OpenMC官方镜像的Claude code安装与配置
## 前言
OpenMC官方镜像包括了OpenMC常用的基础配置，但是缺少很多Claude code的依赖，所以需要手动补齐，后续也会考虑将这些依赖打包成一个镜像，方便后续使用。
本笔记适用于已经有一个能运行的openmc容器的情况下进行。

## 换源
官方镜像容器为 Debian 12（bookworm），将 apt 源替换为清华源。先备份原配置，再写入新源：

```bash
cp /etc/apt/sources.list /etc/apt/sources.list.bak
cat > /etc/apt/sources.list << 'EOF'
# 默认注释了源码镜像以提高 apt update 速度，如有需要可自行取消注释

deb https://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm main contrib non-free non-free-firmware
# deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm main contrib non-free non-free-firmware

deb https://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm-updates main contrib non-free non-free-firmware
# deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm-updates main contrib non-free non-free-firmware

deb https://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm-backports main contrib non-free non-free-firmware
# deb-src https://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm-backports main contrib non-free non-free-firmware

deb https://mirrors.tuna.tsinghua.edu.cn/debian-security bookworm-security main contrib non-free non-free-firmware
# deb-src https://mirrors.tuna.tsinghua.edu.cn/debian-security bookworm-security main contrib non-free non-free-firmware
EOF
apt-get update
```

## 安装必要依赖
```bash
apt-get install -y curl
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt-get install -y nodejs
npm install -g @anthropic-ai/claude-code
```

## 配置 GLM Coding Plan

1. **注册账号**
   访问 [智谱开放平台](https://open.bigmodel.cn/)，点击右上角的「注册/登录」按钮，按照提示完成账号注册流程。

2. **获取 API Key**
   登录后，在个人中心页面，点击 [API Keys](https://bigmodel.cn/usercenter/proj-mgmt/apikeys)，创建一个新的 API Key。
   请妥善保管您的 API Key，不要泄露给他人，也不要直接硬编码在代码中。

3. **配置环境变量**

   通过在 **MacOS、Linux** 或 **Windows** 中使用以下**一种方式**设置环境变量：

   **注意**：设置环境变量时，有些命令不会返回任何输出。这是正常的，只要没有报错即代表成功。

自动化脚本**
   仅支持 MacOS Linux 环境，在终端或 IDE 中运行以下命令即可（⚠️注意此方式不支持 Windows）：
   ```bash
   curl -O "https://cdn.bigmodel.cn/install/claude_code_env.sh" && bash ./claude_code_env.sh
   ```
   脚本会自动通过修改 `~/.claude/settings.json` 来配置环境变量：
   ```json
   {
       "env": {
           "ANTHROPIC_AUTH_TOKEN": "your_zhipu_api_key",
           "ANTHROPIC_BASE_URL": "https://open.bigmodel.cn/api/anthropic",
           "API_TIMEOUT_MS": "3000000",
           "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": 1
       }
   }
   ```
   脚本会自动在 `~/.claude.json` 添加：
   ```json
   {
       "hasCompletedOnboarding": true
   }
   ```
   配置成功后，请确保重新打开一个新的终端窗口，以便环境配置生效。

4. **验证配置**

   配置完成后，进入一个代码工作目录，在终端中执行 `claude` 命令即可开始使用 Claude Code。
   若遇到「Do you want to use this API key」选择 Yes 即可。

   启动后选择信任 Claude Code 访问文件夹里的文件。

   > **注意**：Claude Code v2.1.69 版本 BUG 注意，需要设置环境变量 `ENABLE_TOOL_SEARCH=0 CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS=1` 才能正常使用 claude-opus-4-6。

   **扩展阅读**：更多配置细节请参考 [智谱开放平台 Claude Code 文档](https://docs.bigmodel.cn/cn/coding-plan/tool/claude)。

